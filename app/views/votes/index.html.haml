%h1{:style => "text-align:center"} Which teacher is teacher A?

=link_to image_tag("#{@random[0].image_path}"), professor_path(@random[0])
=link_to image_tag("#{@random[1].image_path}"), professor_path(@random[1])
-#%img(src="#{Professor.find(1).image_path}")= link_to "", v
